export const birdFrames = Array.from(
  { length: 10 },
  (_, index) => `/Frames/Parajaro/${index + 1}.png`,
)

export const vanFrames = Array.from(
  { length: 11 },
  (_, index) => `/Frames/furgo/Frame ${index + 1}.png`,
)

export const grafitiFrames = Array.from(
  { length: 14 },
  (_, index) => `/Frames/grafiti/Frame ${index + 1}.png`,
)

export const jardineroFrames = Array.from(
  { length: 12 },
  (_, index) => `/Frames/jardinero/${index + 1}.png`,
)

export const jardineraFrames = Array.from(
  { length: 15 },
  (_, index) => `/Frames/jardinera/${index + 1}.png`,
)

export const arbolFrames = Array.from(
  { length: 25 },
  (_, index) => `/Frames/arbol/${index + 1}.png`,
)

export const ordenadorFrames = Array.from(
  { length: 9 },
  (_, index) => `/Frames/ordenador/${index + 1}.png`,
)

export const banquitoFrames = Array.from(
  { length: 12 },
  (_, index) => `/Frames/banquito/${index + 1}.png`,
)

export const sofaFrames = Array.from(
  { length: 14 },
  (_, index) => `/Frames/sofa/${index + 1}.png`,
)

export const bibliotecarioFrames = Array.from(
  { length: 8 },
  (_, index) => `/Frames/bibliotecario/${index + 1}.png`,
)

export const cuadroFrames = Array.from(
  { length: 15 },
  (_, index) => `/Frames/cuadro/${index + 1}.png`,
)

export const telefonoFrames = Array.from(
  { length: 8 },
  (_, index) => `/Frames/telefono/${index + 1}.png`,
)

export const parejaFrames = Array.from(
  { length: 11 },
  (_, index) => `/Frames/pareja/${index + 1}.png`,
)

export const personasSillasFrames = Array.from(
  { length: 12 },
  (_, index) => `/Frames/personas_sillas/${index + 1}.png`,
)

export const lectorFrames = Array.from(
  { length: 2 },
  (_, index) => `/Frames/lector/${index + 1}.png`,
)

export const columpioFrames = Array.from(
  { length: 13 },
  (_, index) => `/Frames/columpio/${index + 1}.png`,
)

export const profeFrames = Array.from(
  { length: 16 },
  (_, index) => `/Frames/profe/${index + 1}.png`,
)

export const hombreSentadoFrames = Array.from(
  { length: 9 },
  (_, index) => `/Frames/hombre_sentado/${index + 1}.png`,
)

export const humoFrames = Array.from(
  { length: 26 },
  (_, index) => `/Frames/humo/${index + 1}.png`,
)

export const mujerSentadaFrames = Array.from(
  { length: 11 },
  (_, index) => `/Frames/mujer_sentada/${index + 1}.png`,
)
